<atlassian-plugin key="${atlassian.plugin.key}" name="${project.name}" plugins-version="2">
    <plugin-info>
        <description>${project.description}</description>
        <version>${project.version}</version>
        <vendor name="${project.organization.name}" url="${project.organization.url}" />
        <param name="plugin-icon">images/pluginIcon.png</param>
        <param name="plugin-logo">images/pluginLogo.png</param>
    </plugin-info>

    <!-- add our i18n resource -->
    <resource type="i18n" name="i18n" location="server-side-rendering"/>
    
    <!-- add our web resources -->
    <web-resource key="server-side-rendering-resources" name="server-side-rendering Web Resources">
        <dependency>com.atlassian.auiplugin:ajs</dependency>
        
        <resource type="download" name="server-side-rendering.css" location="/css/server-side-rendering.css"/>
        <resource type="download" name="server-side-rendering.js" location="/js/server-side-rendering.js"/>
        <resource type="download" name="images/" location="/images"/>

        <context>server-side-rendering</context>
    </web-resource>
    
</atlassian-plugin>