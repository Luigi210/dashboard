<atlassian-plugin key="${atlassian.plugin.key}" name="${project.name}" plugins-version="2">
    <plugin-info>
        <description>${project.description}</description>
        <version>${project.version}</version>
        <vendor name="${project.organization.name}" url="${project.organization.url}" />
        <param name="plugin-icon">images/pluginIcon.png</param>
        <param name="plugin-logo">images/pluginLogo.png</param>
    </plugin-info>

    <!-- add our i18n resource -->
    <resource type="i18n" name="i18n" location="client-side-rendering"/>
    
    <!-- add our web resources -->
    <web-resource key="client-side-rendering-resources" name="client-side-rendering Web Resources">
        <dependency>com.atlassian.auiplugin:ajs</dependency>
        
        <resource type="download" name="client-side-rendering.css" location="/css/client-side-rendering.css"/>
        <resource type="download" name="client-side-rendering.js" location="/js/client-side-rendering.js"/>
        <resource type="download" name="images/" location="/images"/>

        <context>client-side-rendering</context>
    </web-resource>
    
</atlassian-plugin>